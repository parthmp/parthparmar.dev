Create the folder manually (don’t rely on the create script):

mkdir psite
cd psite
npm init -y

Add scripts to your package.json:

"scripts": {
  "dev": "nuxt dev",
  "build": "nuxt build",
  "preview": "nuxt preview"
}

Install patch-package first (the missing piece):

npm i patch-package


Then install Nuxt:

npm i nuxt@3.15.0


Run it:

npx nuxt dev